const NOT_NUMBER_MESSAGE = "The value isn't a number";

const divideOp = {
  name: "division",
  symbol: "/"
};

const multiplyOp = {
  name: "multiply",
  symbol: "*"
};

module.exports = {
  NOT_NUMBER_MESSAGE,
  divideOp,
  multiplyOp
};
