const NOT_NUMBER_MESSAGE = "The value isn't a number";
const INVALID_FIRST_VALUE = "First number is not valid";
const INVALID_SECOND_VALUE = "Second number is not valid";
const INVALID_DIVISION_PER_ZERO = "Second number must not be zero";
const DIGIT_NUMBERS = 3;

const divideOp = {
  name: "division",
  symbol: "/"
};

const multiplyOp = {
  name: "multiply",
  symbol: "*"
};

module.exports = {
  INVALID_FIRST_VALUE,
  INVALID_SECOND_VALUE,
  INVALID_DIVISION_PER_ZERO,
  NOT_NUMBER_MESSAGE,
  DIGIT_NUMBERS,
  divideOp,
  multiplyOp
};
